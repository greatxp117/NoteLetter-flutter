# Superseded

This was the Milestone 2 realignment checklist, written 2026-07-05 against contract **1.0.0**.
Tasks 1–7 (config · API layer · models · Firestore layer · read tracking · screens · theming)
landed that week; `git log` has the detail. Its "deferred" list went stale within weeks and nothing
read it, which is why the owed list is now a file scripts read: [`QUEUE.md`](QUEUE.md), driven by
`/flutter-next`, refreshed by `/parity flutter`, specified by
`../NoteLetter-contracts/spec/clients/flutter.md`. CHANGELOG 4.5.0 and `TODO.md` link here by name,
so the file stays as a pointer.
